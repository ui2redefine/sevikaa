/**
 * SW registration is handled automatically by next-pwa (Workbox).
 * next-pwa injects a versioned sw.js at build time, auto-registers it,
 * and handles cache invalidation per deploy — no manual version bumping needed.
 */
export default function SwRegistration() {
  return null;
}
